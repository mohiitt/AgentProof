# HydraDB workstream

Owned by Mohit/Codex. This directory will contain database setup artifacts,
seed ingestion, and query definitions. Application-side adapters belong in
`src/server/hydradb/`; trust reasoning belongs in `src/server/trust/` and
`src/lib/trust/`.

Develop against `rocketride/samples/trust-event.sample.json` until the live
RocketRide pipeline is connected.
