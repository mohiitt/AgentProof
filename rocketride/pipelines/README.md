# RocketRide pipelines

Add `.pipe` files and pipeline-specific documentation here. The first pipeline
should accept `../samples/raw-event.sample.json` and produce the shape shown in
`../samples/trust-event.sample.json`.
